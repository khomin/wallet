info:
  name: coingecko-coins
  type: http
  seq: 7

http:
  method: GET
  url: "{{coinGeckoUri}}/api/v3/coins/tether"
  headers:
    - name: x-cg-pro-api-key
      value: "{{coinGeckoKey}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
