info:
  name: grpc
  type: grpc
  seq: 3

grpc:
  url: "{{grpcUrl}}"
  method: /price.v1.PriceService/ListCoins
  methodType: unary
  message: "{}"
  auth:
    type: oauth2
    flow: authorization_code
    authorizationUrl: http://localhost:8081/realms/whale-tracker/protocol/openid-connect/auth
    accessTokenUrl: http://localhost:8081/realms/whale-tracker/protocol/openid-connect/token
    callbackUrl: http://localhost:5173/callback
    credentials:
      clientId: "{{client-id}}"
      placement: body
    scope: openid profile email
    pkce: {}
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
