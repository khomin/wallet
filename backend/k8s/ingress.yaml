apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whale-tracker-ingress
  namespace: whale-tracker-prod
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: whale-backend
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: whale-frontend
                port:
                  number: 80