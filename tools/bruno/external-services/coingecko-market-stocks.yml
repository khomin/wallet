info:
  name: coingecko-market-stocks
  type: http
  seq: 6

http:
  method: GET
  url: "{{coinGeckoUri}}/api/v3/coins/markets?category=tokenized-stock&order=market_cap_desc&per_page=250&vs_currency=usd"
  headers:
    - name: x-cg-pro-api-key
      value: "{{coinGeckoKey}}"
      disabled: true
  params:
    - name: category
      value: tokenized-stock
      type: query
    - name: order
      value: market_cap_desc
      type: query
    - name: per_page
      value: "250"
      type: query
    - name: vs_currency
      value: usd
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
