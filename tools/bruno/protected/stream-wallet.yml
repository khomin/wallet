info:
  name: stream-wallet
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/v1/wallets/stream"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
