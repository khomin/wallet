info:
  name: get-api-coin
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/coins/eth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
