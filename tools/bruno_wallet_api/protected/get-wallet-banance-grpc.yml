info:
  name: get-wallet-banance-grpc
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/v1/wallets/chain/address/balance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
