info:
  name: get-wallet-assets
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/v1/wallets/assets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
