info:
  name: get-api-prices
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/v1/prices?symbols=btc,trx,ada"
  params:
    - name: symbols
      value: btc,trx,ada
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
