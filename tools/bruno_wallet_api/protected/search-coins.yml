info:
  name: search-coins
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/coins/search"
  body:
    type: json
    data: |-
      {
        "text": "xau"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
