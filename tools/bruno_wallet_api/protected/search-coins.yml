info:
  name: search-coins
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/v1/coins/search"
  body:
    type: json
    data: |-
      {
        "text": "xrp"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
