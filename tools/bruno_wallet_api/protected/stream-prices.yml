info:
  name: stream-prices
  type: http
  seq: 17

http:
  method: GET
  url: "{{url}}/v1/prices/stream"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
