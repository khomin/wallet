info:
  name: post-wallet
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/v1/wallets"
  body:
    type: json
    data: |-
      {
        "chain": "XRP",
        "address": "rJZaP4thmA9FaF2AoXsbYCjUyQa4wkLGzc",
        "token_symbol": "XRP",
        "label": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
