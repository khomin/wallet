info:
  name: create-alert
  type: http
  seq: 14

http:
  method: POST
  url: "{{url}}/v1/alerts"
  body:
    type: json
    data: |-
      {
        "coin_id": "ETH",
        "condition": "CONDITION_ABOVE",
        "price": 1880.45
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
