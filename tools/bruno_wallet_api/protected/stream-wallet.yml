info:
  name: stream-wallet
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/v1/wallets/stream"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
