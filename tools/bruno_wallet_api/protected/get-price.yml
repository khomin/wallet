info:
  name: get-price
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/v1/prices/eth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
