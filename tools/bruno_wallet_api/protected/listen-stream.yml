info:
  name: listen-stream
  type: http
  seq: 16

http:
  method: GET
  url: "{{url}}/v1/prices/stream"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
