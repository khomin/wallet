info:
  name: get-wallets
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/v1/wallets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
