info:
  name: coingecko-categories
  type: http
  seq: 5

http:
  method: GET
  url: "{{coinGeckoUri}}/api/v3/coins/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
